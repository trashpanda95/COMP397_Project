var objects;
(function (objects) {
    var PlayerMovement = /** @class */ (function () {
        //PRIVATE INSTANCE VARIBALES
        //PUBLIC PROPERTIES
        //CONSTRUCTORS
        function PlayerMovement(player) {
        }
        //PRIVATE METHODS
        PlayerMovement.prototype.handleKeyDown = function (event) {
        };
        return PlayerMovement;
    }());
    objects.PlayerMovement = PlayerMovement;
})(objects || (objects = {}));
//# sourceMappingURL=playerMovement.js.map